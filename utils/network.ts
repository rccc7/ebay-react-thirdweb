//Since throughout the application we'll be using the ChainId.Mumbai and importing
//so in order to not commit mistakes when importing, import from a default place
import { ChainId } from "@thirdweb-dev/react";

export default ChainId.Mumbai;